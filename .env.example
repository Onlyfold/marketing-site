# Canonical production URL (no trailing slash). Used for metadataBase, sitemap, robots and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://onlyfold.com

# Optional Google Search Console ownership token (omitted from the page when unset).
GOOGLE_SITE_VERIFICATION=
